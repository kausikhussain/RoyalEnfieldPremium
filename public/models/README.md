# Bike model placeholder

Place the licensed Draco-compressed motorcycle asset at `public/models/bike.glb`.

`BikeModel.tsx` is wired to preload and animate this exact path. The experience ships with a procedural display bike so the page is complete during art-direction and asset production.
